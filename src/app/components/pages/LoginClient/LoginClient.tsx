'use client';

// styles
import styles from './LoginClient.module.scss'

const LoginClient = () => {
	return (
		<div className={styles.sections}>
			<h1>login</h1>
		</div>
	)
}

export default LoginClient;